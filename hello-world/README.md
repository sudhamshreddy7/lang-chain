# In this project we make an API call to ChatGPT using langchain.

**Steps followed:**
1. ``` uv init``` to initialize the python env.
2. added openAI api to the project. make sure the env variable should be "OPENAI_API_KEY" as langchain looks api key which stored under this env variable.

